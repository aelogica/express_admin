#!/bin/bash

EXPRESS_ADMIN_ROOT=$(echo $PWD)

rake test &&\
rake features &&\
cd test/dummy_app && bundle install && bundle exec rake test &&\
cd $EXPRESS_ADMIN_ROOT &&\
cd test/dummy_app_with_devise && bundle install && bundle exec rake test &&\
cd $EXPRESS_ADMIN_ROOT && echo "Done!"
